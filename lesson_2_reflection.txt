initializing a git repository creates a hidden folder ".git" and creates the
files git needs to store it's data.

allows control over what gets committed

staging area allows addition and removal of changed/unchanged files before actually committing